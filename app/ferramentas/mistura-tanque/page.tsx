import type { Metadata } from "next";
import { auth, currentUser } from "@clerk/nextjs/server";
import { db } from "@/db";
import { users, subscriptions } from "@/db/schema";
import { eq } from "drizzle-orm";
import MisturaTanqueClient from "./MisturaTanqueClient";

export const metadata: Metadata = {
  title: "Ordem de Mistura de Tanque | Talhão Digital",
  description: "Descubra a ordem correta de adição de defensivos químicos e fertilizantes foliares no tanque de pulverização para evitar incompatibilidade física e entupimento de bicos.",
};

export default async function MisturaTanquePage() {
  const { userId } = auth();
  const user = await currentUser();
  let isPro = false;
  let userName = "Usuário";

  if (userId && user) {
    userName = user.firstName ? `${user.firstName} ${user.lastName || ""}` : "Usuário";
    const dbUser = await db.query.users.findFirst({
      where: eq(users.id, userId),
    });
    
    let sub = null;
    try {
      sub = await db.query.subscriptions.findFirst({
        where: eq(subscriptions.userId, userId),
      });
    } catch(e) {
      // Ignora erro de tabela não sincronizada
    }

    const isAdmin = user.publicMetadata?.role === 'admin' || dbUser?.role === 'admin';

    if (
      isAdmin || 
      dbUser?.isCourtesyPro || 
      sub?.status === 'active' || 
      sub?.status === 'trialing'
    ) {
      isPro = true;
    }
  }

  return (
    <MisturaTanqueClient 
      isPro={isPro}
      userName={userName} 
    />
  );
}
